nelmio_cors:
    defaults:
        allow_origin: ['*'] # pour dev, plus tard mettre le domaine exact
        allow_methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS']
        allow_headers: ['Content-Type', 'Authorization']
        max_age: 3600
        allow_credentials: false
    paths:
        '^/api/': ~
